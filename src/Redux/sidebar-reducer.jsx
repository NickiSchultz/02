let initialState = {
  friends: [
    { id: 1, name: 'Dimych' },
    { id: 2, name: 'Andre' },
    { id: 3, name: 'Cat' }]
}


export const sidebarReducer = (state = initialState, action) => {
  return state;
}